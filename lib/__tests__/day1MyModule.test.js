const assert = require('assert');
const day1MyModule = require('../index.js');

describe('day1MyModule', () => {
  it('has a test', () => {
    assert(false, 'day1MyModule should have a test');
  });
});
